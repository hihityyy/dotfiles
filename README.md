# dotfiles

My personal configuration files for Arch Linux with the [Niri](https://github.com/YaLTeR/niri) Wayland compositor.

## Contents

[Structure](#structure) · [Dependencies](#dependencies) · [Niri-kb-layout](#niri-kb-layout) · [Fish setup](#fish-setup) · [Wallpapers](#wallpapers)

## Structure

```
dotfiles/
├── bat/
│   ├── config                 # bat config
│   └── themes/
│       └── noctalia.tmTheme
├── cava/
│   ├── config                 # cava config
│   ├── shaders/                # visualizer shaders
│   └── themes/
│       ├── noctalia
│       ├── solarized_dark
│       └── tricolor
├── fastfetch/
│   ├── arch_logo.png          # png arch_logo
    ├── config.jsonc           # fastfetch config
    ├── hihitylogo.txt         # my own arch_logo
    └── textlogo               # asci text ArchLinux
├── fish/
│   ├── config.fish            # shell config
│   ├── fish_plugins           # fisher plugin list (incl. tide)
│   ├── include/
│   │   ├── alias.fish
│   │   ├── environment.fish
│   │   └── function.fish
│   ├── completions/           # fisher/tide (generated, not hand-edited)
│   ├── conf.d/                 # fisher/tide (generated, not hand-edited)
│   └── themes/
├── kitty/
│   ├── kitty.conf             # terminal emulator config
│   ├── current-theme.conf -> themes/noctalia.conf
│   └── themes/
│       └── noctalia.conf
├── lazygit/
│   ├── config.yml             # lazygit config
│   └── themes/
│       └── noctalia.yml
├── niri/
│   ├── config.kdl             # wayland compositor config
│   ├── noctalia.kdl           # noctalia-shell integration
│   ├── niri-kb-layout         # auto-switch layout daemon (binary)
│   ├── niri-kb-layout.cpp     # source
│   └── include/
│       ├── animations.kdl
│       ├── binds.kdl
│       ├── debug.kdl
│       ├── environment.kdl
│       ├── gestures.kdl
│       ├── hotkey-overlay.kdl
│       ├── input.kdl
│       ├── layer-rule.kdl
│       ├── layout.kdl
│       ├── output.kdl
│       ├── spawn-at-startup.kdl
│       └── window-rule.kdl
├── noctalia/
│   ├── settings.toml           # noctalia-shell config
│   ├── state.toml
│   ├── community-palettes/     # extra color palettes
│   ├── community-templates/    # theming templates for other apps
│   └── plugins/                # installed noctalia plugins
│                                 # (plus generated cache/history dirs, not tracked by hand)
├── nvim/
│   ├── init.lua                # neovim config (LazyVim-based)
│   ├── lazy-lock.json
│   ├── lazyvim.json
│   ├── matugen_colors.lua      # matugen-generated color scheme
│   ├── stylua.toml
│   └── lua/
│       ├── config/
│       │   ├── autocmds.lua
│       │   ├── keymaps.lua
│       │   ├── lazy.lua
│       │   └── options.lua
│       ├── matugen-template.lua
│       ├── matugen.lua
│       └── plugins/
│           ├── base16.lua
│           ├── colorscheme.lua
│           └── dashboard.lua
├── yazi/
│   ├── flavors/
│   │   └── noctalia.yazi/
│   ├── keymap.toml             # keybindings
│   ├── theme.toml
│   └── yazi.toml               # main config
├── Wallpapers/                 # wallpaper collection (i-01.png ... i-16.png)
├── preview.png
└── README.md
```

> `fish/completions` and `fish/conf.d` are mostly generated by **fisher**/**tide** — don't edit them by hand, see [Fish setup](#fish-setup) below. Likewise, most of `noctalia/` beyond `settings.toml` is runtime state managed by noctalia-shell itself.

## Dependencies

```sh
sudo pacman -S niri kitty fish yazi zoxide lsd bat cava lazygit xdg-desktop-portal-gnome celluloid eog neovim nlohmann-json jq
paru -S noctalia-git
```

> [Noctalia](https://docs.noctalia.dev) v5 is installed from the AUR (`noctalia-git`) — replace `paru` with your AUR helper of choice.

## Niri-kb-layout

> **AI-generated code.**

Background daemon that auto-switches keyboard layout to English when focusing a **Kitty** terminal window.

**How it works:**
- Listens to `niri msg --json event-stream` in the background
- When focus moves to **Kitty** and current layout is **Russian** → switches to **English**
- When focus leaves Kitty and Russian was active before → restores **Russian**
- If already **English** → does nothing

**Build:**
```sh
g++ -std=c++20 niri/niri-kb-layout.cpp -o niri/niri-kb-layout
```

**Run (background):**
Add to `niri/include/spawn-at-startup.kdl`:
```kdl
spawn-at-startup "niri-kb-layout"
```

Or run manually:
```sh
./niri/niri-kb-layout &
```

## Fish setup

Prompt theme is [tide](https://github.com/IlanCosman/tide), installed via [fisher](https://github.com/jorgebucaran/fisher).

```sh
# install fisher (plugin manager)
curl -sL https://raw.githubusercontent.com/jorgebucaran/fisher/main/functions/fisher.fish | source
fisher install jorgebucaran/fisher

# install plugins from fish_plugins (this pulls in tide)
fisher update
```

If tide needs reconfiguring afterwards:

```sh
tide configure
```

## Wallpapers

[![preview](./preview.png)](./preview.png)
